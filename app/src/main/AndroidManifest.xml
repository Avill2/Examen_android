<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andreavillacis.examen_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".conductor" />
        <activity android:name=".Factory" />
        <activity android:name=".crear_conductorActivity" />
        <activity android:name=".Listar_conductoresActivity" />
        <activity android:name=".DetalleActivity" />
        <activity android:name=".AutoParcelable" />
        <activity android:name=".ConductorParcelable" />
        <activity android:name=".detalle_AutoActivity"></activity>
    </application>

</manifest>